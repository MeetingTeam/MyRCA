# Lightweight requirements for K8s tasks (drift_detection, preprocess, model_comparison, model_deploy)
# No torch — only needed for AWS Batch training jobs. sklearn pulled by Evidently; pin to match GPU image.
numpy>=1.24.0
pandas>=2.0.0
joblib>=1.3.0
clickhouse-driver>=0.2.0
pyarrow>=14.0.0
mlflow>=2.12.0
boto3>=1.28.0
scikit-learn==1.6.1

# Evidently for drift detection (pinned version for stability)
evidently==0.4.33

# Kubernetes Python client for model_deploy (ConfigMap patch + rolling restart)
kubernetes>=27.2.0
