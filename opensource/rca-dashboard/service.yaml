apiVersion: v1
kind: Service
metadata:
  name: rca-dashboard
  namespace: rca
spec:
  selector:
    app: rca-dashboard
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30880
  type: NodePort
