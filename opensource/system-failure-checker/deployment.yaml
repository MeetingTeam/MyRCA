apiVersion: apps/v1
kind: Deployment
metadata:
  name: system-failure-checker
  namespace: rca
  labels:
    app: system-failure-checker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: system-failure-checker
  template:
    metadata:
      labels:
        app: system-failure-checker
    spec:
      containers:
        - name: system-failure-checker
          image: hungtran679/system-failure-checker:dev
          imagePullPolicy: Always
          envFrom:
            - configMapRef:
                name: system-failure-checker-configmap
            - secretRef:
                name: system-failure-checker-secret
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /healthcheck
              port: 8001
            initialDelaySeconds: 5
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 10
          readinessProbe:
            httpGet:
              path: /healthcheck
              port: 8001
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 5
            failureThreshold: 10
      restartPolicy: Always